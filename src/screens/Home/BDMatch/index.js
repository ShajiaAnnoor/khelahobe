import React, { 
	useEffect,
} from "react";
import { View } from "react-native";

const BDMatch = ({
	slug, 
	flag
}) => {

	useEffect(
		() => {
		},
		[
		]
	);

	return (
		<View>
		</View>
	)
}

export default BDMatch
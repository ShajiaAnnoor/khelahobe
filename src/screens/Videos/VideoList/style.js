import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	container: {
		flex: 1,
		alignItems: 'center',
		justifyContent: 'center',
		paddingTop:15,
		margin:2,
		padding:10,
	},

	headline: {
		fontSize:20,
		fontWeight:'bold',
		paddingTop:2,
		marginTop:0,
		marginBottom:30,
	}
});
import React from 'react';
import { 
  useSelector, 
  useDispatch,
} from 'react-redux';
import { 
  SafeAreaView, 
  View,
} from 'react-native';

import { styles } from "./Livescore.style";

const LivescoreScreenComponent = ({ navigation }) => { 
    return (
        <SafeAreaView >
        </SafeAreaView>
  );
};

export default LivescoreScreenComponent;
